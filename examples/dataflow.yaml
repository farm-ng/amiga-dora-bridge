nodes:
  #- id: dora-amiga-camera
  #  build: pip install -e ../dora_amiga
  #  path: dora-amiga-camera
  #  inputs:
  #    tick: dora/timer/millis/30
  #  outputs:
  #    - oak0/rgb
  #    - oak1/rgb
  - id: dora-amiga-gps
    build: pip install -e ../dora_amiga
    path: dora-amiga-gps
    inputs:
      tick: dora/timer/millis/100
    outputs:
      - gps/pvt
  - id: dora-amiga-canbus
    build: pip install -e ../dora_amiga
    path: dora-amiga-canbus
    inputs:
      tick: dora/timer/millis/100
    outputs:
      - canbus/motor_states